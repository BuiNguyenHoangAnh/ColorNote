<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:title="@string/search"
        android:id="@+id/menusearch"
        app:showAsAction="always"
        android:icon="@drawable/ic_search_white">
    </item>
    <item android:title="@string/optionmenu"
        app:showAsAction="always"
        android:icon="@drawable/ic_more" >
        <menu>
            <item android:id="@+id/today"    android:title="@string/today" android:icon="@drawable/ic_calendar"/>
            <item android:id="@+id/color"    android:title="@string/color" android:icon="@drawable/ic_color"/>
        </menu>
    </item>
</menu>